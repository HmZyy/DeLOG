/target
.worktrees/
*.swp
.cache/

PLAN.md
docs/superpowers/

# Local-only: machine-specific pyo3/libpython interpreter pin for the
# optional `scripting` feature (absolute paths differ per machine).
.cargo/config.toml
