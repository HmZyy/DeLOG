//! Scripts window: REPL + editor + library (SCR-07). Feature-gated.

/// All UI + engine state for the scripts window. (Fleshed out in Task 14.)
#[derive(Default)]
pub struct ScriptsPanel {
    pub open: bool,
}
